---
import { Img } from "astro-imagetools/components";

export interface Props {
	alt: string;
	caption: string;
	src: string;
}

const { alt, caption, src } = Astro.props as Props;
---

{
	caption ? (
		<figure class="bg-white rounded-md overflow-hidden not-prose">
			<Img src={src} alt={alt} placeholder="none" />
			<figcaption class="m-2 text-sm text-gray-500 dark:text-gray-100">
				{caption}
			</figcaption>
		</figure>
	) : (
		<div class="flex justify-center bg-white rounded-md not-prose overflow-hidden">
			<Img src={src} alt={alt} placeholder="none" />
		</div>
	)
}
